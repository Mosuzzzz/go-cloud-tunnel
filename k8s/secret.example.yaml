# คัดลอกไฟล์นี้เป็น secret.yaml แล้วใส่ค่าจริง (secret.yaml ถูก gitignore ไว้)
apiVersion: v1
kind: Secret
metadata:
  name: app-secrets
  namespace: big-project
type: Opaque
stringData:
  DB_USER: "user"
  DB_PASSWORD: "change-me"
  DB_NAME: "url_db"
  API_KEY: "change-me"
